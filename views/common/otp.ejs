<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>OTP</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <style>
    *{
      margin: 0;
      padding: 0;
      font-family: sans-serif;
    }
    body{
      background-color: lightblue;
    }
    .main{
      height: 450px;
      width: 300px;
      background-color: #ffb703;
      position: absolute;
      top: 100px;
      left: 40%;
      border-radius: 20px;
    }
    .main h1{
      font-size: 35px;
      text-align: center;
      margin-top: 20px;
    }
    .main p{
      font-size: 15px;
      position: relative;
      top:130px ;
      text-align: center;
    }
    .main input{
      height: 50px;
      width: 50px;
      position: relative;
      top: 150px;
      left: 14%;
      border-radius: 50%;
      border: 1px solid gray;
      text-align: center;
      font-size: larger;
      color: white;
      background-color: black;
    }
    .main input:focus{
      outline: 1px solid yellow;
    }
    .main input:valid{
      background-color: yellow;
      color: black;
    }
    .submit{
      position: relative;
      top: 170px;
      left: 10%;
      height: 40px;
      width: 80%;
      border-radius: 10px;
      border: 1px solid gray;
      background-color: green;
      font-size: larger;
      color: white;
    }
    .resent{
      font-size: large;
      position: relative;
      top: 220px;
      left: 10%;
    }
    .resent button{
      background:none;
      color: blue;
      font-size: large;
      border: none;
      margin-left: 10px;
    }
    .resent button:hover{
      transform: scale(1.1);
    }
    .fa-landmark{
      position: absolute;
      top: 100px;
      left: 45%;
      color: cyan;
      font-size: 50px;
    }
    .main p span{
      color: red;
      position: relative;
      top: 5px;
      font-size: 15px;
    }
  </style>
</head>
<body>
  <div class="main">
    <h1>OTP verification</h1>
    <i class="fa-solid fa-landmark"></i>
    <p>Enter the OTP received to <span> <%= reqBody.email %> </span></p>
    <form action="/otp" method="post" id="otpForm">
      <input type="text" class="input" name="num1" pattern="[0-9]*" maxlength="1"  autofocus  >
      <input type="text" class="input" name="num2" pattern="[0-9]*" maxlength="1"   >
      <input type="text" class="input" name="num3" pattern="[0-9]*" maxlength="1"   >
      <input type="text" class="input" name="num4" pattern="[0-9]*" maxlength="1"   >
      <button type="submit" class="submit">verify</button>
    </form>
    <div class="resent">
      Didn't recive otp ?
      <button id="resent">resent otp</button>
    </div>
  </div>
  <script>
    const form = document.getElementById("otpForm");

    form.addEventListener("input", function (event) {
        const target = event.target;
        if (target.tagName.toLowerCase() === "input" && target.value.length === 1) {
            const nextInput = target.nextElementSibling;
            if (nextInput && nextInput.tagName.toLowerCase() === "input") {
                nextInput.focus();
            }
        }
    });

    form.addEventListener("submit", function (event) {
        event.preventDefault(); 
        form.submit();
    });
</script>
</body>
</html>